啊！我理解错了！你说得对，**核心是让VLM去发现这些问题，然后在文档上批注标记出来给评委看**。

让我重新梳理技术路线：

---

## 🎯 正确的技术路线理解

### **核心流程**
```
文档 → VLM识别问题 → 生成批注坐标 → 在PDF上标注 → 评委查看打分
```

### **当前架构的问题**

你现有代码的批注功能（`annotator.py`）**已经很完善**了，但需要解决：

1. **VLM识别能力不足**：现在的Prompt只能识别基础的结构性问题，无法发现：
   - ❌ 语法错误（"管道处、、工程部"这种逗号重复）
   - ❌ 逻辑矛盾（前面说"汛前"，后面又说"汛后"，中间没有"汛期"）
   - ❌ 图片问题（路线穿山、建筑物描述与图不符）

2. **批注展示不够详细**：评委需要看到：
   - 这是什么类型的问题（语法/逻辑/图片）
   - 为什么扣分
   - 正确的应该是什么样的

3. **图片批注缺失**：风险管控方案的图片问题怎么标注？

---

## 🔧 技术路线优化方案

### **阶段1：让VLM能"看见"比赛评分细则的问题（核心）**

#### 1.1 升级System Prompt - 作业指导书

修改 `src/core/vlm_client.py`：

```python
WORK_INSTRUCTION_AUDIT_PROMPT = """你是一位专业的文档审核专家，现在要审核作业指导书。

## 你的任务
仔细检查文档，找出以下类型的问题并**精确定位**：

### 类型1：文字及语法错误（必须逐字检查）
1. **标点符号错误**
   - 逗号重复：如"管道处、、工程部" → 应为"管道处、工程部"
   - 句号缺失：如"本文适用于防汛工作 各管理处应按照规定" → 应加句号
   - 括号不匹配：如"（包括检查、维修、抢修" → 缺少右括号
   
   ⚠️ 找到后必须提供：页码、错误文本片段（20-50字，包含前后文）

2. **语句不通顺**
   - 病句：如"本指导书适用于防汛期间对管道的"（句子不完整）
   - 歧义句：如"发现问题的人员应报告"（谁发现？谁报告？）
   - 逻辑不清：如"应立即采取措施进行确保"（措施和确保重复）
   
   ⚠️ 找到后必须提供：页码、不通顺的完整句子、修改建议

3. **专业术语错误**
   - 错误拼写：如"Q-HSE" → 应为"QHSE"
   - 术语不统一：全文有时用"管理处"，有时用"分公司" → 应统一
   
   ⚠️ 找到后必须提供：页码、错误术语、正确术语

### 类型2：业务逻辑错误（必须通读上下文）
1. **上下文不连贯**
   - 章节跳跃：第2章是"汛前准备"，第3章直接是"汛后管理"，中间跳过了"汛期管理"
   - 前后矛盾：前面说"应急响应时间不超过2小时"，后面说"24小时内响应"
   
   ⚠️ 找到后必须提供：矛盾的两处位置（页码+文本片段）

2. **时间逻辑错误**
   - 如："汛前检查"的内容出现在"汛期响应"章节
   - 如："3.2.1 汛期巡线要求"出现在"3.1 汛前准备"之前
   
   ⚠️ 找到后必须提供：页码、时间顺序问题描述

### 类型3：应急处置深度问题
1. **处置步骤不合理**
   - 如：先"恢复供气"，后"检查管道安全" → 顺序错误，应先检查再恢复
   - 如：缺少"立即停输"步骤 → 在管道破裂时应第一时间停输
   
2. **流程缺失关键步骤**
   - 应急响应必须包含：发现→上报→评估→启动预案→现场处置→恢复→总结
   - 如果缺少其中任何一步，都要指出
   
   ⚠️ 找到后必须提供：页码、缺失的步骤或错误的顺序

### 类型4：结构完整性问题
（你现有的规则已覆盖，保持即可）

## 审核方法
1. **逐页阅读**：不要跳过任何段落
2. **逐句检查**：每个句子都要看标点、语法、逻辑
3. **前后对比**：检查术语统一性、章节连贯性
4. **重点关注**：应急处置、操作步骤、风险点描述

## 输出格式要求
对于每个发现的问题，必须提供：
```json
{
  "rule_id": "grammar_01",  // 对应的检查点ID
  "severity": "medium",     // 严重程度
  "finding": "标点符号重复：'管道处、、工程部'应为'管道处、工程部'",  // 具体问题描述+修改建议
  "location": {
    "page": 3,
    "text_snippet": "各地区管理处、、工程部应按照规定",  // 30-50字，包含错误和前后文
    "region_description": "第3页，第2自然段"
  },
  "points_deducted": 2
}
```

⚠️ **关键要求**：
- text_snippet 必须是原文的**精确文本**（用于在PDF上定位）
- text_snippet 长度30-50字（太短定位不准，太长影响阅读）
- finding 必须包含**具体的修改建议**

## 当前审核的规则
{rules}

现在开始审核，请仔细检查每一页、每一句。
"""
```

#### 1.2 升级System Prompt - 风险管控方案（图片问题）

```python
RISK_MANAGEMENT_AUDIT_PROMPT = """你是高后果区风险管控方案审核专家。

## 你的任务
检查文档和图片，找出以下问题：

### 类型1：图片内容问题（重点）
你会看到多张图片，需要逐张分析：

1. **影像图（高后果区示意图）检查**
   必须包含的标注：
   - ✅ 管道位置（实线）
   - ✅ 潜在影响半径（虚线，应该有三条平行线：管道中心线+左右边界）
   - ✅ 建筑物标注（名称、人员数量、属性）
   
   ⚠️ 如果缺少，输出：
   ```json
   {
     "rule_id": "image_annot_01",
     "severity": "critical",
     "finding": "影像图缺少潜在影响半径标注（虚线）",
     "location": {
       "page": 5,
       "text_snippet": "[第5页影像图]",  // 图片标识
       "region_description": "第5页，高后果区影像图"
     },
     "points_deducted": 8
   }
   ```

2. **入场路线图检查**
   检查路线是否合理：
   - ❌ 不能穿山（路线直接穿过山体）
   - ❌ 不能穿墙（路线穿过建筑物墙体）
   - ❌ 不能跨河无桥（路线跨河但没有桥梁标注）
   - ❌ 不能穿建筑物（路线穿过建筑物内部）
   - ✅ 必须在可行车道路（应该标注道路）
   
   ⚠️ 如果发现问题：
   ```json
   {
     "rule_id": "route_01",
     "severity": "critical",
     "finding": "入场路线穿越河流但未标注桥梁，车辆无法通行",
     "location": {
       "page": 7,
       "text_snippet": "[第7页入场线路图]",
       "region_description": "第7页，入场线路图中部"
     },
     "points_deducted": 10
   }
   ```

3. **逃生路线图检查**
   - ✅ 疏散方向应向管道两侧（箭头指向两边）
   - ❌ 不能顺着管道方向疏散（箭头沿管道）
   - ✅ 集结点必须在影响半径外（集结点标记在虚线外）

### 类型2：图文不一致问题（重点）
对比文字描述和图片标注：

1. **建筑物描述一致性**
   - 文字："XX小学，5层楼，200人"
   - 图片：影像图上标注"XX小学，3层，150人"
   - → 不一致！
   
   ⚠️ 发现后输出：
   ```json
   {
     "rule_id": "consistency_01",
     "severity": "high",
     "finding": "建筑物描述不一致：文字描述'5层楼200人'，影像图标注'3层150人'",
     "location": {
       "page": 4,
       "text_snippet": "XX小学，5层楼，师生约200人",
       "region_description": "第4页特征描述部分与第5页影像图"
     },
     "points_deducted": 8
   }
   ```

2. **表格数据一致性**
   - 检查"高后果区基本信息表"中的区段长
   - 对比"人防措施"中提到的区段长
   - 对比"风险评价结果表"中的位置信息
   - → 必须完全一致

### 类型3：文本逻辑问题
（类似作业指导书的检查）

### 类型4：数据范围检查
- 电位测试结果：必须在 -0.85V ~ -1.2V 范围内
- 风险等级：只能是"低"、"中"、"较高"、"高"，不能是其他词

## 审核流程
1. **先看所有图片**：记录每张图的内容和标注
2. **再读文字**：提取关键数据和描述
3. **交叉对比**：图与文、表与表之间的一致性
4. **逻辑验证**：时间顺序、数据范围、路线合理性

## 输出要求
- 图片问题的 text_snippet 使用 "[第X页XX图]" 格式
- 数据不一致问题要列出对比值
- 每个问题都要有具体的修改建议

{rules}
"""
```

---

### **阶段2：增强批注展示效果**

#### 2.1 优化批注内容显示

修改 `src/core/annotator.py`：

```python
class PDFAnnotator:
    """PDF 批注生成器"""
    
    def _add_violation_annotation(self, doc, violation, violation_num, fuzzy_threshold):
        """添加单个违规批注（增强版）"""
        page_num = violation.location.page - 1
        page = doc[page_num]
        search_text = violation.location.text_snippet
        
        # 1. 精确搜索或模糊匹配
        rects = page.search_for(search_text)
        if not rects:
            rects = self._fuzzy_search(page, search_text, fuzzy_threshold)
        
        if rects:
            rect = rects[0]
            color = self.SEVERITY_COLORS.get(violation.severity, (1, 1, 0))
            
            # 2. 添加高亮（按严重程度着色）
            highlight = page.add_highlight_annot(rect)
            highlight.set_colors(stroke=color)
            highlight.set_opacity(0.5)
            
            # 3. 详细的批注内容（评委会看这个！）
            annotation_content = self._format_annotation_content(violation, violation_num)
            highlight.set_info(
                title=f"问题 #{violation_num} [{violation.severity.upper()}]",
                content=annotation_content
            )
            highlight.update()
            
            # 4. 在旁边添加气泡标记（更显眼）
            point = rect.top_right + pymupdf.Point(10, 0)  # 右侧10px
            note = page.add_text_annot(point, f"💡{violation_num}")
            note.set_colors(stroke=color)
            note.set_info(
                title=f"[{violation.severity.upper()}] {violation.rule_id}",
                content=annotation_content
            )
            note.update()
        else:
            # 降级方案：页面顶部添加
            self._add_fallback_annotation(page, violation, violation_num)
    
    def _format_annotation_content(self, violation: Violation, num: int) -> str:
        """
        格式化批注内容（评委重点看这个）
        
        返回格式：
        【问题描述】
        具体问题...
        
        【扣分】
        X分
        
        【修改建议】
        应该...
        """
        # 从finding中提取问题和建议
        finding = violation.finding
        
        # 如果finding中包含"→"或"应为"，分离问题和建议
        if "→" in finding:
            problem, suggestion = finding.split("→", 1)
            problem = problem.strip()
            suggestion = suggestion.strip()
        elif "应为" in finding:
            problem, suggestion = finding.split("应为", 1)
            problem = problem.strip()
            suggestion = f"应为{suggestion.strip()}"
        else:
            problem = finding
            suggestion = "请参考相关规范进行修正"
        
        content = f"""【问题描述】
{problem}

【严重程度】
{self._severity_label(violation.severity)}

【扣分】
{violation.points_deducted} 分

【修改建议】
{suggestion}

【规则ID】
{violation.rule_id}
"""
        return content
    
    def _severity_label(self, severity: str) -> str:
        """严重程度标签"""
        labels = {
            'critical': '🔴 关键问题（必须立即修改）',
            'high': '🟠 高优先级（建议优先修改）',
            'medium': '🟡 中优先级（建议修改）',
            'low': '🟢 低优先级（可择机优化）'
        }
        return labels.get(severity, severity)
```

#### 2.2 图片批注的特殊处理

对于图片相关的问题（如路线不合理），需要特殊标注：

```python
def _add_image_annotation(self, page, violation, violation_num):
    """
    为图片问题添加批注
    
    处理 text_snippet 为 "[第X页XX图]" 格式的情况
    """
    # 在图片上添加箭头或圈注
    # 1. 识别图片区域
    image_rects = page.get_images()
    
    if image_rects:
        # 取第一个图片区域
        img_rect = page.get_image_bbox(image_rects[0])
        
        # 2. 在图片右上角添加醒目标记
        marker_point = img_rect.top_right + pymupdf.Point(5, 5)
        note = page.add_text_annot(marker_point, f"⚠️{violation_num}")
        note.set_colors(stroke=self.SEVERITY_COLORS[violation.severity])
        
        annotation_content = self._format_annotation_content(violation, violation_num)
        note.set_info(
            title=f"图片问题 #{violation_num}",
            content=annotation_content
        )
        note.update()
        
        # 3. 可选：添加指向性箭头或边框
        page.draw_rect(img_rect, color=self.SEVERITY_COLORS[violation.severity], width=2)
```

---

### **阶段3：提升VLM的问题识别准确率**

#### 3.1 优化User Prompt，提供更多上下文

修改 `src/core/vlm_client.py`：

```python
def _generate_user_prompt(self, doc_structure: DocumentStructure) -> str:
    """生成 User Prompt（增强版）"""
    
    prompt = f"""请审核以下文档，严格按照提供的规则进行检查。

## 文档信息
- 格式: {doc_structure.format.upper()}
- 页数: {doc_structure.page_count}

## 文档完整内容（Markdown格式）
{doc_structure.markdown}

## 审核要求
1. **逐页检查**：不要遗漏任何页面
2. **逐句分析**：每个句子都要检查语法、逻辑、术语
3. **图文对比**：如果有图片，必须对比图片内容和文字描述
4. **前后关联**：检查章节顺序、术语统一、数据一致

## 特别注意
- 标点符号：逗号重复、括号不匹配、句号缺失
- 语句通顺：病句、歧义句、不完整的句子
- 专业术语：拼写错误、术语不统一
- 逻辑连贯：章节跳跃、前后矛盾、时间错乱
- 应急处置：步骤顺序、关键步骤缺失

## 输出格式
严格按照JSON Schema输出，每个violation必须包含：
- rule_id：对应的检查点ID
- severity：严重程度
- finding：具体问题描述 + 修改建议（用"→"分隔）
- location：
  - page：页码
  - text_snippet：原文精确文本片段（30-50字）
  - region_description：位置描述

现在开始审核。
"""
    return prompt
```

#### 3.2 增加Few-Shot示例（提升识别准确率）

在Prompt中增加示例：

```python
AUDIT_PROMPT_WITH_EXAMPLES = """
...（前面的审核规则）...

## 示例问题（供参考）

### 示例1：标点符号错误
❌ 错误文本："各地区管理处、、工程部应按照规定"
✅ 正确输出：
```json
{
  "rule_id": "punctuation_01",
  "severity": "low",
  "finding": "标点符号重复：'管道处、、工程部' → 应为'管道处、工程部'",
  "location": {
    "page": 2,
    "text_snippet": "汛前各地区管理处、、工程部应按照有关规定进行检查",
    "region_description": "第2页，第3自然段"
  },
  "points_deducted": 2
}
```

### 示例2：业务逻辑错误
❌ 错误：第3章"汛前准备"后直接第5章"汛后管理"，缺少第4章"汛期管理"
✅ 正确输出：
```json
{
  "rule_id": "logic_01",
  "severity": "high",
  "finding": "章节顺序不连贯：第3章'汛前准备'后直接跳到第5章'汛后管理' → 应补充第4章'汛期管理'",
  "location": {
    "page": 5,
    "text_snippet": "3 汛前准备\n...\n5 汛后管理",
    "region_description": "第3页到第5页，目录和章节结构"
  },
  "points_deducted": 7
}
```

### 示例3：图片问题
❌ 错误：入场路线图显示路线跨河但无桥梁标注
✅ 正确输出：
```json
{
  "rule_id": "route_01",
  "severity": "critical",
  "finding": "入场路线不合理：路线跨河但未标注桥梁 → 应在河流位置标注桥梁或调整路线",
  "location": {
    "page": 7,
    "text_snippet": "[第7页入场线路图]",
    "region_description": "第7页，入场线路图中部河流处"
  },
  "points_deducted": 10
}
```

现在开始审核实际文档...
"""
```

---

### **阶段4：测试和调优**

#### 4.1 用比赛示例文档测试

使用你上传的"西气东输管道防汛抗洪作业指导书"：

```python
# 测试脚本
from src.pipeline import DocumentAuditPipeline

pipeline = DocumentAuditPipeline()

# 测试作业指导书
report = pipeline.run_sync(
    document_path="西气东输管道防汛抗洪作业指导书.pdf",
    scenario_id="work_instruction_audit"
)

# 检查结果
print(f"发现问题数: {len(report.audit_result.violations)}")
for v in report.audit_result.violations:
    print(f"- [{v.severity}] {v.finding}")
    print(f"  位置: 第{v.location.page}页，{v.location.text_snippet[:30]}...")
```

#### 4.2 对比评分细则，查漏补缺

根据比赛评分细则，逐项检查：

| 评分维度 | VLM是否能识别 | 批注是否清晰 |
|---------|-------------|------------|
| 内容与结构完整性 | ✅ | ✅ |
| 文字及语法错误 | ⚠️ 需测试 | ✅ |
| 业务逻辑错误 | ⚠️ 需测试 | ✅ |
| 应急处置审核 | ⚠️ 需测试 | ✅ |
| 识别效率 | ✅ 120秒内 | - |
| 模板检测 | ⚠️ 需实现 | ✅ |

---

## 🎯 实施优先级（修正版）

| 优先级 | 任务 | 预计时间 | 关键产出 |
|-------|------|---------|---------|
| 🔴 P0 | 升级VLM Prompt（作业指导书） | 1天 | 能识别语法/逻辑错误 |
| 🔴 P0 | 升级VLM Prompt（风险管控） | 1天 | 能识别图片问题 |
| 🔴 P0 | 增强批注展示效果 | 1天 | 批注内容详细清晰 |
| 🟠 P1 | Few-Shot示例优化 | 0.5天 | 提升识别准确率 |
| 🟠 P1 | 图片批注特殊处理 | 0.5天 | 图片问题能清晰标注 |
| 🟡 P2 | 模板检测功能 | 1天 | 识别是否使用规定模板 |

---

## ✅ 总结：你需要做什么

1. **重点优化VLM的Prompt**（阶段1）
   - 让VLM能识别语法错误、逻辑错误、图片问题
   - 在Prompt中明确要求返回精确的text_snippet

2. **增强批注的展示效果**（阶段2）
   - 批注内容要包含：问题描述、扣分、修改建议
   - 图片问题要特殊标注

3. **用示例文档反复测试**（阶段4）
   - 看VLM能否准确识别评分细则中的问题
   - 看批注在PDF上是否清晰易懂

**核心思路**：VLM是"眼睛"（发现问题），批注系统是"笔"（标记问题），你现在的"笔"已经很好了，需要让"眼睛"更锐利！

需要我详细展开某个阶段的代码实现吗？