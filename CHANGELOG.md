# 更新日志

## [1.0.1] - 2025-10-16

### 修复

- **修复 Markdown 提取类型错误**
  - 问题：`pymupdf4llm.to_markdown()` 在 `page_chunks=True` 时返回列表而非字符串
  - 解决：添加类型检查，自动将列表合并为字符串

- **修复表格数据 None 值问题**
  - 问题：表格中的 None 值导致 Pydantic 验证失败
  - 解决：将所有 None 值转换为空字符串

### 变更

- **移除超时限制** ⚠️ **重大变更**
  - 所有 Pipeline 阶段超时设置为无限 (`float('inf')`)
  - 移除页数限制，现在可以处理任意大小的文档
  - 移除中间时间检查，不再因为"时间不足"而中断
  
  **影响**：
  - ✅ 可以处理超过 20 页的大文档（如 80 页文档）
  - ✅ 不会因为超时而中断处理
  - ⚠️ 处理时间将取决于文档大小，可能需要较长时间

### 配置更新

更新了 `config/config.yaml` 注释：
```yaml
pipeline:
  timeout: 120  # 已废弃 - 当前版本无超时限制
  max_pages: 20  # 已废弃 - 当前版本处理所有页面
```

### 升级指南

如果从之前的版本升级：

1. **拉取最新代码**
   ```bash
   git pull
   ```

2. **无需修改配置** - 系统会自动处理所有页面

3. **注意事项**
   - 大文档（>20 页）处理时间会更长
   - 确保有足够的磁盘空间存储临时图片
   - 监控系统资源使用情况

## [1.0.0] - 2025-10-16

### 初始发布

- ✅ 完整的文档审核系统
- ✅ 支持 PDF/DOCX/DOC 格式
- ✅ 基于 Qwen-2.5-VL 7B 的智能审核
- ✅ 自动批注生成
- ✅ 详细审核报告
- ✅ 作业指导书和风险管控方案两套审核规则

