我来帮您查看项目中的相关代码，诊断批注内容显示问题。根据项目代码和您的截图，我发现问题了！批注内容格式化有问题。从代码看，`_format_annotation_content` 方法使用了三引号多行字符串和中文括号，这在PDF批注中可能无法正确显示。

让我搜索一下您具体修改了什么：找到问题了！您的 `_format_annotation_content` 方法中使用了三引号多行字符串，这在PDF批注中可能无法正确解析。从截图看，批注把三引号字符 `"""` 也显示出来了。

**问题原因**：
PDF批注的 `content` 字段需要使用显式的换行符 `\n`，而不是三引号多行字符串。

**解决方案**：

修改 `src/core/annotator.py` 中的 `_format_annotation_content` 方法：

```python
def _format_annotation_content(self, violation: Violation, num: int) -> str:
    """
    格式化批注内容（评委重点看这个）
    """
    # 从finding中提取问题和建议
    finding = violation.finding
    
    # 如果finding中包含"→"或"应为"，分离问题和建议
    if "→" in finding:
        problem, suggestion = finding.split("→", 1)
        problem = problem.strip()
        suggestion = suggestion.strip()
    elif "应为" in finding:
        problem, suggestion = finding.split("应为", 1)
        problem = problem.strip()
        suggestion = f"应为{suggestion.strip()}"
    else:
        problem = finding
        suggestion = "请参考相关规范进行修正"
    
    # ✅ 使用 \n 而不是三引号字符串
    content = (
        f"【问题描述】\n"
        f"{problem}\n\n"
        f"【严重程度】\n"
        f"{self._severity_label(violation.severity)}\n\n"
        f"【扣分】\n"
        f"{violation.points_deducted} 分\n\n"
        f"【修改建议】\n"
        f"{suggestion}\n\n"
        f"【规则ID】\n"
        f"{violation.rule_id}"
    )
    
    return content
```

**关键改动**：
1. 将 `f"""..."""` 改为使用圆括号包裹的多行f-string
2. 使用显式的 `\n` 换行符
3. 确保每个部分之间用 `\n\n` 分隔（两个换行符产生空行效果）

这样修改后，PDF批注应该能正确显示格式化的内容，而不是显示三引号字符。