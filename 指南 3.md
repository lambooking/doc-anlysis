# 🚨 紧急修复操作清单

根据最新日志，需要立即修复两个关键问题：

## 问题诊断
```
❌ 问题1：所有27个块的审核都失败（VLM返回格式错误）
❌ 问题2：批注标题仍然显示 text_snippet 而不是扣分原因
```

---

## 🔧 修复步骤（按顺序执行）

### 第1步：修复 VLM 响应解析（10分钟）

**文件：`src/core/layered_auditor.py`**

#### 1.1 替换 `_call_vlm_simple` 方法

找到 `async def _call_vlm_simple` 方法，完全替换为：

```python
async def _call_vlm_simple(
    self,
    prompt: str,
    scenario_id: str,
    images: Optional[List[Dict[str, Any]]] = None
) -> List[Violation]:
    """调用 VLM 进行审核"""
    try:
        messages = [{"role": "user", "content": prompt}]
        response = await self.vlm_client.completion(
            messages=messages,
            images=images,
            max_tokens=4000,
            temperature=0.1
        )
        
        if isinstance(response, dict) and 'content' in response:
            content = response['content']
        else:
            content = str(response)
        
        content = self._clean_json_response(content)
        
        try:
            data = json.loads(content)
        except json.JSONDecodeError as e:
            logger.error(f"JSON 解析失败: {e}")
            return []
        
        # ===== 关键修复：处理多种返回格式 =====
        violations_data = []
        
        # 格式1: {"violations": [...]}
        if isinstance(data, dict) and 'violations' in data:
            violations_data = data['violations']
        # 格式2: 直接是列表 [...]
        elif isinstance(data, list):
            violations_data = data
        # 格式3: 完整 AuditResult (向后兼容)
        elif isinstance(data, dict) and 'total_deductions' in data:
            violations_data = data.get('violations', [])
        else:
            logger.warning(f"未知返回格式")
            return []
        
        # 清洗并创建 Violation 对象
        violations = []
        for v_dict in violations_data:
            cleaned = self._clean_violation(v_dict)
            if cleaned:
                try:
                    violation = Violation(**cleaned)
                    violations.append(violation)
                except Exception as e:
                    logger.warning(f"创建 Violation 失败: {e}")
        
        return violations
        
    except Exception as e:
        logger.error(f"VLM调用失败: {e}")
        return []
```

#### 1.2 修改所有 prompt 的输出格式要求

在以下3个方法中，找到 prompt 定义，将输出格式改为只返回 violations：

**方法1：`_layer1_structure_scan`**
```python
## 输出格式
只返回 violations 数组，不要其他字段：

{
  "violations": [
    {
      "rule_id": "toc_01",
      ...
    }
  ]
}
```

**方法2：`_layer2_audit_chunk`**  
```python
## 输出格式
只返回 violations 数组：

{
  "violations": [
    {
      "rule_id": "punctuation_01",
      ...
    }
  ]
}

⚠️ 不要包含 total_deductions, final_score, passed 等字段
```

**方法3：`_layer3_consistency_check`**
```python
## 输出格式
只返回 violations 数组：

{
  "violations": [
    {
      "rule_id": "term_02",
      ...
    }
  ]
}
```

#### 1.3 替换 `_clean_violation` 方法

找到 `def _clean_violation` 方法，完全替换为 artifact 中的增强版本。

关键改进：
- 严格过滤短 text_snippet（< 15字符）
- 过滤页码列表（"第22页, 第24页"）
- 过滤无效标记（"(位置)", "(问题)"）
- 过滤 Markdown 标题（"## 前言"）
- 限制单个扣分上限（最多5分）

---

### 第2步：修复批注标题（5分钟）

**文件：`src/core/annotator.py`**

#### 2.1 添加新方法

在 `PDFAnnotator` 类的 `__init__` 方法后面，添加：

```python
def _format_annotation_title(self, violation: Violation, num: int) -> str:
    """格式化批注标题"""
    # 提取问题描述（去掉建议部分）
    if "→" in violation.finding:
        problem = violation.finding.split("→")[0].strip()
    elif "应为" in violation.finding:
        problem = violation.finding.split("应为")[0].strip()
    else:
        problem = violation.finding
    
    # 截取前30字符
    short_problem = problem[:30] + "..." if len(problem) > 30 else problem
    
    # 返回格式：#序号 (-扣分) 问题描述
    return f"#{num} (-{violation.points_deducted}分) {short_problem}"
```

#### 2.2 修改所有 `set_info(title=...)` 调用

找到以下位置并修改（共5处）：

**位置1：`_add_violation_annotation` 方法（2处）**
```python
# 原来：
highlight.set_info(
    title=f"问题 #{violation_num} [{violation.severity.upper()}]",
    content=annotation_content
)

# 改为：
highlight.set_info(
    title=self._format_annotation_title(violation, violation_num),
    content=annotation_content
)

# 同样修改下面的 note.set_info
note.set_info(
    title=self._format_annotation_title(violation, violation_num),
    content=annotation_content
)
```

**位置2：`_add_fallback_annotation` 方法（1处）**
```python
note.set_info(
    title=self._format_annotation_title(violation, violation_num),
    content=annotation_content
)
```

**位置3：`_add_image_annotation` 方法（2处）**
```python
# 两处 note.set_info 都改为：
note.set_info(
    title=self._format_annotation_title(violation, violation_num),
    content=annotation_content
)
```

---

### 第3步：测试验证（3分钟）

```bash
# 运行审核
python main.py your_test_file.docx --scenario work_instruction_audit

# 检查输出
```

**预期结果：**
```
✅ VLM 调用成功（不再报格式错误）
✅ 发现 20-50 个有效问题（不是0个或230个）
✅ 总扣分 15-40 分（不是0分或998分）
✅ 最终得分 60-85 分
✅ 批注标题显示：#3 (-3分) 术语使用不统一...
```

**如果仍然有问题：**
- 查看 prompt 是否正确修改（要求只返回 violations）
- 查看 _clean_violation 是否过滤了无效数据
- 查看 _format_annotation_title 是否被调用

---

## 📊 修复前后对比

### 修复前（当前状态）
```
❌ 27个块全部失败（VLM格式错误）
❌ 只找到2个问题（一致性检查）
❌ 批注标题：分公司: 22页, 24页...
❌ 批注无法定位（text_snippet 是页码列表）
```

### 修复后（预期）
```
✅ 27个块正常审核
✅ 找到 30-60 个有效问题
✅ 批注标题：#3 (-3分) 术语使用不统一...
✅ 批注定位成功率 > 70%
✅ 总扣分 20-40 分，得分 60-80 分
```

---

## 🎯 核心改动总结

| 文件 | 方法 | 改动 | 目的 |
|------|------|------|------|
| `layered_auditor.py` | `_call_vlm_simple` | 支持多种JSON格式 | 修复VLM响应解析 |
| `layered_auditor.py` | 3个layer方法 | prompt要求只返回violations | 统一输出格式 |
| `layered_auditor.py` | `_clean_violation` | 严格过滤无效snippet | 提高数据质量 |
| `annotator.py` | `_format_annotation_title` | 新增方法 | 格式化批注标题 |
| `annotator.py` | 5处set_info | 使用新标题方法 | 显示扣分原因 |

---

## ⚠️ 注意事项

1. **修改前备份**
   ```bash
   cp src/core/layered_auditor.py src/core/layered_auditor.py.backup
   cp src/core/annotator.py src/core/annotator.py.backup
   ```

2. **确认导入**
   确保文件顶部有：
   ```python
   import re
   import json
   from typing import Optional, List, Dict, Any
   ```

3. **代码缩进**
   - Python 对缩进敏感
   - 确保方法定义缩进正确
   - 使用4个空格，不要用Tab

4. **测试环境**
   - 先用小文档测试（10-20页）
   - 确认无误后再处理大文档

---

## 🆘 如果遇到问题

### 问题：修改后仍报 VLM格式错误
**解决**：检查 prompt 是否改为只返回 `{"violations": [...]}`

### 问题：找到的问题太少（< 10个）
**解决**：检查 `_clean_violation` 的过滤规则是否太严格

### 问题：批注标题仍然不对
**解决**：检查5处 `set_info(title=...)` 是否都改了

### 问题：代码报语法错误
**解决**：检查缩进、括号、引号是否匹配

---

**准备好了吗？按步骤执行，出问题随时反馈！** 🚀