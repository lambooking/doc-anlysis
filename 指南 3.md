# 审核系统优化 - 快速实施指南

## 🎯 优化目标

1. ✅ **降低扣分严格程度** - 让评分更合理
2. ✅ **优化批注标题** - 从 `[MEDIUM] term_01` 改为 `#3 (-2分) 术语不统一...`
3. ✅ **减少过度扣分** - 总扣分控制在合理范围

---

## 📋 实施步骤

### 步骤 1：修改批注标题（5分钟）

1. 打开文件：`src/core/annotator.py`

2. 在 `PDFAnnotator` 类中，找到 `__init__` 方法后面，添加新方法：

```python
def _format_annotation_title(self, violation: Violation, num: int) -> str:
    """格式化批注标题，让评委一眼看懂"""
    # 提取问题描述（去掉修改建议部分）
    if "→" in violation.finding:
        problem = violation.finding.split("→")[0].strip()
    elif "应为" in violation.finding:
        problem = violation.finding.split("应为")[0].strip()
    else:
        problem = violation.finding
    
    # 截取前25个字符
    short_problem = problem[:25] + "..." if len(problem) > 25 else problem
    
    # 组装标题：序号 + 扣分 + 问题
    return f"#{num} (-{violation.points_deducted}分) {short_problem}"
```

3. 查找所有 `set_info(title=` 的地方（约有 5-6 处），将 title 参数改为：

```python
# 原来：
title=f"[{violation.severity.upper()}] {violation.rule_id}"

# 改为：
title=self._format_annotation_title(violation, violation_num)
```

**具体位置：**
- `_add_violation_annotation` 方法中：2处
- `_add_fallback_annotation` 方法中：1处
- `_add_image_annotation` 方法中：2处

---

### 步骤 2：批量降低扣分值（3分钟）

1. 创建文件：`tools/reduce_deductions.py`（从上面的 artifact 复制代码）

2. 运行脚本：
```bash
cd /path/to/your/project
python tools/reduce_deductions.py
```

3. 查看生成的文件：`config/audit_rules_optimized.yaml`

4. 如果满意，替换原文件：
```bash
# 备份原文件
cp config/audit_rules.yaml config/audit_rules_backup.yaml

# 使用优化后的配置
cp config/audit_rules_optimized.yaml config/audit_rules.yaml
```

---

### 步骤 3：测试验证（5分钟）

1. 用一个测试文档运行审核：
```bash
python main.py --input test.docx --scenario work_instruction_audit
```

2. 检查结果：
   - 打开生成的 PDF，查看批注标题是否改为新格式
   - 查看审核报告，确认总扣分是否合理（建议在 20-40 分之间）
   - 如果总分还是太低，可以再次运行脚本，调整 `REDUCTION_FACTOR`

---

## 🔧 微调选项

### 如果觉得扣分还是太多：

修改 `tools/reduce_deductions.py` 中的参数：

```python
# 原来
REDUCTION_FACTOR = 0.4  # 降低到40%

# 改为更宽松
REDUCTION_FACTOR = 0.3  # 降低到30%（更宽松）
```

### 如果觉得扣分太少：

```python
REDUCTION_FACTOR = 0.5  # 降低到50%（稍微严格一点）
```

### 批注标题长度调整：

在 `_format_annotation_title` 方法中修改：

```python
# 原来：截取25个字符
short_problem = problem[:25] + "..." if len(problem) > 25 else problem

# 改为更长：
short_problem = problem[:35] + "..." if len(problem) > 35 else problem

# 改为更短：
short_problem = problem[:20] + "..." if len(problem) > 20 else problem
```

---

## 📊 预期效果对比

### 修改前 ❌

**批注标题：**
```
[MEDIUM] term_01
```
- 看不懂具体问题
- 需要点开才知道扣了多少分

**扣分情况：**
- 发现 15 个问题
- 总扣分：68 分
- 最终得分：32 分 ❌ 太低

---

### 修改后 ✅

**批注标题：**
```
#3 (-2分) 术语不统一："管理处"和"分公司"混用
```
- 一目了然
- 扣分明确
- 问题清晰

**扣分情况：**
- 发现 15 个问题
- 总扣分：27 分
- 最终得分：73 分 ✅ 合理

---

## ⚠️ 注意事项

1. **修改前务必备份**
   ```bash
   cp src/core/annotator.py src/core/annotator.py.backup
   cp config/audit_rules.yaml config/audit_rules.yaml.backup
   ```

2. **建议分步测试**
   - 先只修改批注标题，测试一次
   - 再调整扣分规则，测试一次
   - 确保每一步都符合预期

3. **保留原配置文件**
   - 不要删除 `audit_rules_backup.yaml`
   - 如果需要回滚，可以快速恢复

4. **团队沟通**
   - 如果有多人使用，记得同步更新
   - 统一配置文件版本

---

## 🎨 批注标题效果示例

| 原标题（修改前） | 新标题（修改后） |
|---|---|
| `[MEDIUM] term_01` | `#1 (-2分) 专业术语拼写错误："QHSE"误写为"Q-HSE"` |
| `[HIGH] safety_02` | `#2 (-3分) 未标注危险源和风险点` |
| `[CRITICAL] emergency_01` | `#3 (-4分) 应急步骤顺序不合理：先恢复生产后确认安全` |
| `[LOW] fmt_01` | `#4 (-1分) 标题字体不符合规范：一级标题应使用黑体` |

---

## 📞 需要帮助？

如果遇到问题，检查：
1. Python 版本是否 ≥ 3.8
2. 是否安装了 PyYAML：`pip install pyyaml`
3. 文件路径是否正确
4. 是否有文件写入权限

---

**祝使用愉快！如有问题随时反馈。** 🎉